const BaseHandler = require('./../BaseHandler')

class ScheduleBookingHandler extends BaseHandler {
  constructor(options) {
    super(options)
  }

  async execute(page) {
    // @TODO: book selected schedule
  }
}

module.exports = ScheduleBookingHandler